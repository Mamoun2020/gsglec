// to use it with any class that inherit from another class (multi inheritance), can use function read
// when call name mixin by (with) keyword
mixin Reading{
  read(String message){
    print(message);
  }
}